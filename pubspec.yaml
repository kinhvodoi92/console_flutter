name: console_flutter
description: Plugin to show API logs for Flutter. Includes a console screen to view logs. Export logs as a zip file.
version: 1.1.0
homepage: "https://github.com/kinhvodoi92"
repository: "https://github.com/kinhvodoi92/console_flutter"

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  logz: ^0.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter: